## 1. 事前調査

- [ ] 1.1 `frontend/e2e/stock-items.spec.ts` の既存パターン（アイテム作成・削除・セレクタ）を確認する
- [ ] 1.2 `frontend/src/components/FilterBar.tsx` を読み、検索ボックス・カテゴリドロップダウン・買い物リストボタンのセレクタ（aria-label / role）を確認する
- [ ] 1.3 `frontend/src/app/stock-items/StockItemsClient.tsx` を読み、ログアウトボタン（`aria-label="サインアウト"`）とログアウト後のリダイレクト先を確認する
- [ ] 1.4 ItemCard の編集ボタン（aria-label なし）を E2E で特定するセレクタを決定する（テキスト内容または CSS クラスベース）

## 2. stock-items.spec.ts への追加

- [ ] 2.1 D-1: 商品を作成 → 編集ボタンをクリックして EditItemModal を開く → 名前を変更して「保存」→ カードに新しい名前が表示されることを検証するテストを追加する（`// Scenario: D-1` コメント付き）
- [ ] 2.2 D-2: 商品を作成 → 編集ボタンをクリックして EditItemModal を開く → カテゴリを変更して「保存」→ カードのカテゴリバッジが更新されることを検証するテストを追加する（`// Scenario: D-2` コメント付き）
- [ ] 2.3 K-4: `/stock-items` を表示 → `aria-label="サインアウト"` ボタンをクリック → URL が `/login` に変わることを検証するテストを追加する（`// Scenario: K-4` コメント付き）

## 3. filter.spec.ts の新規作成

- [ ] 3.1 `frontend/e2e/filter.spec.ts` を作成し、テスト用アイテムのセットアップ・クリーンアップヘルパーを実装する（異なるカテゴリ・wantToBuy 状態の商品を少なくとも 3 件用意する）
- [ ] 3.2 F-1: 商品名の一部を検索ボックスに入力すると一致する商品だけ表示され、非一致商品が非表示になることを検証するテストを追加する（`// Scenario: F-1` コメント付き）
- [ ] 3.3 F-2: 検索テキストが入力された状態でクリアボタンを押すと全商品が再表示されることを検証するテストを追加する（`// Scenario: F-2` コメント付き）
- [ ] 3.4 F-3: 「買いたいものだけ」ボタンをクリックすると wantToBuy=true の商品だけが表示されることを検証するテストを追加する（`// Scenario: F-3` コメント付き）
- [ ] 3.5 F-4: カテゴリドロップダウンで特定カテゴリを選択すると、そのカテゴリの商品だけが表示されることを検証するテストを追加する（`// Scenario: F-4` コメント付き）
- [ ] 3.6 F-5: 検索テキストとカテゴリフィルタを同時に適用したとき、両方の条件を満たす商品だけが表示されることを検証するテストを追加する（`// Scenario: F-5` コメント付き）

## 4. CI 確認

- [ ] 4.1 変更を push して `gh pr checks --watch` で CI (e2e job) が green になることを確認する
